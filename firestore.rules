rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 認証なし版: すべてのドキュメントに読み書き許可（開発用）
    // ⚠️ 警告: 本番環境では認証を実装してください
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

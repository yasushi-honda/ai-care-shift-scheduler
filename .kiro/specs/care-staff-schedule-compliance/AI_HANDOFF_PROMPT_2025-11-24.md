# 🤖 AI引き継ぎプロンプト - Phase 25完了 → Phase 26開始

**作成日**: 2025-11-24
**プロジェクト**: ai-care-shift-scheduler
**現在地**: Phase 25完了、Phase 26準備完了

---

## 📌 このプロンプトの使い方

**新しいAIセッションの最初に、このプロンプト全体をコピー＆ペーストしてください。**

---

# プロジェクト引き継ぎ - Phase 25完了状況

## プロジェクト概要

**プロジェクト名**: ai-care-shift-scheduler（介護事業所向けシフト管理システム）

**目的**: AIシフト自動生成＋実績管理の効率化

**開発状況**:
- **完了Phase**: Phase 0-25（基盤構築、AI統合、ユーザビリティ改善）
- **総マイルストーン**: 25以上
- **総開発期間**: 約2ヶ月（2025-10-23開始）

**最新成果**:
- 実績入力作業時間を**86%削減**（50分/月 → 7分/月）
- GitHub Pagesでクライアント・開発者・AI向けドキュメント基盤構築

**URL**:
- 本番環境: https://ai-care-shift-scheduler.web.app
- GitHub Pages: https://yasushi-honda.github.io/ai-care-shift-scheduler/
- リポジトリ: https://github.com/yasushi-honda/ai-care-shift-scheduler

---

## Phase 25 完了サマリー

### 実装した機能

#### 改善1: 「予定と同じ」ボタン
- **完了日**: 2025-11-23
- **効果**: 50%削減（50分/月 → 25分/月）
- **ファイル**: `src/components/schedule/CopyScheduledButton.tsx`

#### 改善2: 一括コピー機能
- **完了日**: 2025-11-24
- **効果**: 86%削減（50分/月 → 7分/月）
- **ファイル**: `src/components/schedule/BulkCopyScheduledToActualModal.tsx`

#### GitHub Pages ドキュメント
- **完了日**: 2025-11-24
- **内容**:
  - [index.html](docs/index.html): クライアント向け（プロジェクト変遷・規模・ロードマップ）
  - [technical.html](docs/technical.html): 開発者向け（WBS・技術詳細）
- **機能**: Mermaid図12個、ズーム・スクロール、ダークモード対応

---

## Phase 26 推奨タスク

### 優先度: 高

#### 1. E2Eテスト追加（推定4時間）
**対象**: 改善1・2のPlaywright E2Eテスト

**参考ファイル**:
- `tests/e2e/auth.spec.ts` - 既存パターン
- メモリ: `phase14_e2e_test_patterns.md`

**実装例**:
```typescript
// tests/e2e/bulk-copy-scheduled-to-actual.spec.ts
describe('一括コピー機能', () => {
  it('複数スタッフの予定を実績にコピーできる', async ({ page }) => {
    await loginAsManager(page);
    await page.goto('/schedule');
    await page.click('button:has-text("一括コピー")');
    await page.click('input[type="checkbox"][value="staff1"]');
    await page.click('input[type="checkbox"][value="staff2"]');
    await page.fill('input[name="startDate"]', '2025-11-01');
    await page.fill('input[name="endDate"]', '2025-11-07');
    await page.click('button:has-text("実行")');
    await expect(page.locator('.toast-success')).toBeVisible();
  });
});
```

#### 2. モバイル最適化（推定1週間）
**対象**: レスポンシブデザイン（768px以下）、タッチ操作

---

## 重要な技術的注意事項（必読）

### ⚠️ CRITICAL: Gemini APIリージョン
**必ず`us-central1`を使用すること**

```typescript
// ✅ 正しい
region: 'us-central1'

// ❌ 間違い（403エラー発生）
region: 'asia-northeast1'
```

詳細: メモリ `gemini_region_critical_rule.md`

### ⚠️ GitHub Pages Mermaid記法
**Pure HTML形式**を使用

```html
<!-- ✅ 正しい -->
<div class="mermaid">
graph TB
  A --> B
</div>

<!-- ❌ 間違い（Jekyllがエスケープ） -->
\`\`\`mermaid
graph TB
  A --> B
\`\`\`
```

### ⚠️ Firebase CLI認証エラー
認証エラー発生時は即座に**GitHub Actions CI/CD**に切り替える

詳細: `.kiro/steering/development-workflow.md`

---

## 開発ワークフロー

### GitHub Flow
```bash
# 1. 新機能ブランチ作成
git checkout -b feature/phase26-task

# 2. 実装＋コミット
git commit -m "feat: Phase 26機能実装"

# 3. CodeRabbitレビュー（必須）
coderabbit review --plain --base-commit HEAD~1 --config CLAUDE.md

# 4. プッシュ＋PR作成
git push origin feature/phase26-task
gh pr create --title "..." --body "..."
```

### CI/CDパイプライン
1. `git push` → GitHub Actions
2. Lighthouse CI → 品質チェック
3. Firebase Deploy → 本番デプロイ
4. GitHub Pages Deploy → ドキュメント公開

---

## 必読ドキュメント（優先順）

### 最優先（必ず読むこと）
1. **`project_overview.md`** - プロジェクト全体概要（メモリ）
2. **`phase25_progress_2025-11-24_final.md`** - Phase 25進捗（メモリ）
3. **`phase25_design_decisions_2025-11-24_final.md`** - 設計判断記録（メモリ）
4. **`PHASE26_HANDOFF_2025-11-24.md`** - Phase 26引き継ぎ（`.kiro/specs/`）

### 実装時に参照
5. **`phase14_e2e_test_patterns.md`** - E2Eテストパターン（メモリ）
6. **`code_style_and_conventions.md`** - コーディング規約（メモリ）
7. **`tech_stack.md`** - 技術スタック（メモリ）

### トラブルシューティング
8. **`gemini_region_critical_rule.md`** - Gemini APIリージョン（メモリ）
9. **`firebase_cli_error_handling.md`** - Firebase CLI問題（メモリ）

---

## プロジェクト構造（重要ファイルのみ）

```
ai-care-shift-scheduler/
├── src/
│   ├── components/schedule/
│   │   ├── BulkCopyScheduledToActualModal.tsx  ← Phase 25で追加
│   │   └── ScheduleView.tsx  ← Phase 25で修正
│   └── services/firestore/
│       └── actualShifts.ts  ← 一括コピーロジック
├── tests/e2e/
│   └── bulk-copy.spec.ts  ← Phase 26で追加予定
├── docs/  ← GitHub Pages
│   ├── index.html
│   └── technical.html
└── .kiro/
    ├── specs/care-staff-schedule-compliance/
    │   ├── phase25-2.5-completion-2025-11-24.md
    │   ├── PHASE26_HANDOFF_2025-11-24.md
    │   └── AI_HANDOFF_PROMPT_2025-11-24.md  ← 本ファイル
    └── steering/
        └── development-workflow.md
```

---

## よくある質問（FAQ）

### Q1: Phase 25の実装コードはどこ？
**A**:
- `src/components/schedule/BulkCopyScheduledToActualModal.tsx`
- `src/components/schedule/ScheduleView.tsx`
- コミット: `e80f5d1`, `f551c3e`

### Q2: GitHub Pagesの更新方法は？
**A**:
```bash
vim docs/index.html
git add docs/
git commit -m "docs: GitHub Pages更新"
git push origin main
```

### Q3: E2Eテストの実行方法は？
**A**:
```bash
# ローカルサーバー起動（別ターミナル）
PORT=5173 npm run dev

# E2Eテスト実行
PLAYWRIGHT_BASE_URL=http://localhost:5173 npm run test:e2e
```

### Q4: メモリファイルの読み方は？
**A**:
```
メモリファイルは自動的に利用可能です。
必要に応じて「project_overview.mdを確認してください」と指示してください。
```

---

## Phase 26完了の定義

以下をすべて満たすこと：

### 機能実装
- [ ] E2Eテスト追加（改善1・2）
- [ ] モバイル最適化（768px以下対応）
- [ ] 改善3実装（ダブルクリック機能）

### ドキュメント
- [ ] Phase 26完了記録作成（`.kiro/specs/`）
- [ ] GitHub Pages更新（ロードマップ反映）
- [ ] プロジェクトメモリ更新

### テスト
- [ ] E2Eテスト全パス
- [ ] モバイルブラウザで動作確認
- [ ] Lighthouse CI成功

### デプロイ
- [ ] 本番環境デプロイ完了
- [ ] GitHub Pages更新確認

---

## 最初のステップ（推奨）

1. **プロジェクト状態確認**
   ```bash
   git status
   git log --oneline -10
   ```

2. **メモリファイル確認**
   - `project_overview.md` を読む
   - `phase25_progress_2025-11-24_final.md` を読む

3. **Phase 26タスク選択**
   - E2Eテスト追加から開始（推奨）
   - または、ユーザーに次のタスクを確認

4. **ドキュメントドリブン開発**
   - タスク開始前に計画ドキュメント作成
   - 実装完了後に完了記録作成

---

## 次のAIへのメッセージ

Phase 25で実績入力作業時間を**86%削減**し、GitHub Pagesでドキュメント基盤を構築しました。

Phase 26では**E2Eテスト追加**と**モバイル最適化**を推奨します。

すべてのドキュメントは`.kiro/`配下とメモリに記録済みです。

**ドキュメントドリブンで計画的に進めてください。**

**成功を祈ります！**🚀

---

## 📝 このプロンプトを使った後の確認

新しいAIセッションでこのプロンプトを使った後、以下を確認してください：

1. ✅ プロジェクト概要を理解したか？
2. ✅ Phase 25の完了内容を把握したか？
3. ✅ Phase 26の推奨タスクを理解したか？
4. ✅ 重要な技術的注意事項を確認したか？
5. ✅ 開発ワークフローを理解したか？

すべて✅なら、Phase 26の実装を開始してください！
